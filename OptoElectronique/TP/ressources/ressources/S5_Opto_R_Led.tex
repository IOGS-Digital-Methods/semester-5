\newpage
\pagestyle{empty}

\begin{minipage}[c]{.25\linewidth}
	\includegraphics[width=4cm]{images/Logo-LEnsE.png}
\end{minipage} \hfill
\begin{minipage}[c]{.4\linewidth}

\begin{center}
\vspace{0.3cm}
{\Large \textsc{Opto-Électronique}}

\medskip

\textbf{\Large Ressources}

\end{center}
\end{minipage}\hfill

\vspace{0.5cm}

\noindent \rule{\linewidth}{1pt}
\section{Calculer la résistance de protection d'une diode (LED)}
\label{ressource:RLed}

Les \textbf{diodes} sont des \textbf{composants non linéaires} capables de \textbf{laisser passer du courant dans une direction}. Il n'existe cependant \textbf{aucune limitation naturelle de ce courant} si ce n'est la destruction du composant par effet Joule. Le fabricant donne alors une intensité maximale à ne pas dépasser (ou une puissance maximale que le composant peut dissiper) pour conserver les propriétés initiales du composant.

Afin de limiter ce courant, qu'on nommera $i_F$ (F pour \textit{Forward} ou direct), il est \textbf{indispensable} d'ajouter une \textbf{résistance de protection} en série avec une LED comme dans le schéma de la figure~\ref{fig:r_led_man}.


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%
\begin{figure}[h!]
    \centering
\begin{circuitikz}
	\draw (0,3) to[short, -] (3,3);
	% fleche
	\draw (0.5,0.3) edge[->] (0.5,2.7);
	\node (Ein) at (0,1.5){$u_E$};
	
	\draw (5,3) to[empty diode, invert] (2,3);
	\draw (2,3) to[short, i>=$ $, current arrow scale=8] ++(0.5,0);
	\node (Ein) at (2,3.5){$i_F$};
	\draw (4.5,2.3) edge[->, green!40!black] (2.5,2.3); 
	\node[text=green!40!black] (US) at (3.5,2){$u_D$};

	\draw (5,3) to[R=$R_{P}$, *-*] (5,0) -- (0,0);
	\draw (5,0) to[short, -o] ++(1.5,0);
	\draw (5,3) to[short, -o] ++(1.5,0);
	\draw (5,0) node[ground](GND){};
	% fleche
	\draw (6.5,0.3) edge[->, green!40!black] (6.5,2.7); 
	\node[text=green!40!black] (US) at (7.1,1.5){$u_R$};
	
\end{circuitikz}
    \caption{Schéma du circuit de protection d'une LED.}
    \label{fig:r_led_man}
\end{figure}


\subsection{Caractéristiques importantes}

Les principales caractéristiques à relever sont : 

\begin{description}
	\item[$I_{Fmax}$] courant direct maximal admissible par la diode, ou $P_{max}$ puissance maximale que le composant peut dissiper (selon les constructeurs),
	\item[$V_F$] tension directe lorsque la diode est parcourue par un courant.
\end{description}

\subsection{Équations autour du montage}

Les lois classiques de l'électrocinétique (mailles, noeuds, Ohm...) donnent les équations suivantes pour le circuit précédent :

$$u_e = u_d + u_R \qquad u_R = R_P * i_F$$

On obtient alors : $$i_F = \frac{u_e - u_d}{R_P}$$

Le courant $i_F$ est maximal lorsque la diode est passante. On pourra faire l'hypothèse, dans ce cas, que la tension $u_d$ à ces bornes vaut $V_F$.

Ainsi, l'équation précédente devient : $$i_F = \frac{u_e - V_F}{R_P}$$

Le but est que le courant $i_F$ soit toujours inférieur à $I_{Fmax}$, on obtient alors l'inégalité suivante :

$$\boxed{\frac{u_e - V_F}{R_P} < I_{Fmax}}$$

\subsection{Calcul de la résistance de protection minimale}

L'inégalité précédente donne : 

$$R_P > \frac{u_e - V_F}{I_{Fmax}}$$

Le terme de droite est maximal lorsque $u_E$ est maximal. $u_E$ correspond à la tension d'entrée de ce montage. Sa valeur est limitée par le générateur utilisé pour produire cette différence de potentiel.

On choisira alors $u_e = U_{emax}$ pour le calcul de la valeur minimale de $R_P$.

{\Large
$$\boxed{R_P > \frac{U_{emax} - V_F}{I_{Fmax}}}$$
}

\bigskip

Il est possible alors de prendre une \textbf{marge de sécurité sur la valeur de la résistance} $R_P$. Cependant si cette marge est trop grande, le courant maximal serait très en dessous de la valeur limite et le composant (type LED) pourrait ne pas fonctionner correctement (loin de sa valeur nominale).

\bigskip

Il peut également être intéressant de vérifier la \textbf{puissance} que va dissiper la résistance.

$$P = R_P * I_{Fmax}$$